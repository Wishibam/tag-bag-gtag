version: 2

updates:
    - commit-message:
          include: "scope"
          prefix: "composer"
      directory: "/"
      package-ecosystem: "composer"
      schedule:
          interval: "monthly"
      versioning-strategy: "increase-if-necessary"

